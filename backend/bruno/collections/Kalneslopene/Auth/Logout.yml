info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/api/auth/logout"
  headers:
    - name: X-XSRF-TOKEN
      value: "{{csrfToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
