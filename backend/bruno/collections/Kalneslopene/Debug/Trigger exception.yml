info:
  name: Trigger exception
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/debug/error"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
