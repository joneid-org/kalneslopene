info:
  name: Create
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/api/races"
  body:
    type: json
    data: |-
      [
          {
              "raceDate": "2026-04-19T18:00:00Z",
              "weather": "Cloudy"
          }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
