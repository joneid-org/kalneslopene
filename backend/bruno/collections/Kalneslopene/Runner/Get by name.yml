info:
  name: Get by name
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/runners?name=Erik"
  params:
    - name: name
      value: Erik
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

