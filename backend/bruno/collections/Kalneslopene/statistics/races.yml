info:
  name: races
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/statistics/races"
  params:
    - name: year
      value: "2025"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
